# Core dependencies for NFC Collection

# WebSocket server
aiohttp==3.8.5
python-socketio==5.9.0
aiohttp-cors==0.7.0

# Configuration
python-dotenv==1.0.0

# Hardware libraries (install on Pi only)
# These are installed by setup-pi.sh:
# adafruit-blinka
# adafruit-circuitpython-pn532
# adafruit-circuitpython-neopixel
# rpi-lgpio