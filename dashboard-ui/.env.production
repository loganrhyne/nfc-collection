# Production environment variables
# When building for Pi deployment, the WebSocket should connect to the Pi's address
# Update this to match your Pi's hostname or IP
REACT_APP_WS_URL=http://192.168.1.114:8000